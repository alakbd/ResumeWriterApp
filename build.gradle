// In your project-level build.gradle
buildscript {
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2' // Your current AGP
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.22" // Update Kotlin to match
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
