<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/padding_large"
        android:gravity="center_horizontal">

        <!-- Title -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/TitleText"
            android:text="Admin Login"
            android:layout_marginBottom="@dimen/margin_large"/>


        <!-- Benefit / Info Text -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="This app helps job seekers create professional CVs quickly and track their usage credits. Admins can monitor users and manage credits."
            android:textSize="@dimen/text_small"
            android:textColor="@color/text_secondary"
            android:gravity="center"
            android:layout_marginBottom="@dimen/margin_large"/>

        <!-- Email -->
        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/AppEditText"
            android:id="@+id/et_admin_email"
            android:hint="Enter admin email"
            android:layout_marginBottom="@dimen/margin_medium"/>
        
        <!-- Password -->
        <EditText
            style="@style/AppEditText"
            android:id="@+id/et_admin_password"
            android:hint="Enter admin password"
            android:inputType="textPassword"
            android:layout_marginBottom="@dimen/margin_large"/>

        <!-- Login Button -->
        <Button
            style="@style/PrimaryButton"
            android:id="@+id/btn_admin_login"
            android:text="Login as Admin"
            android:layout_marginBottom="@dimen/margin_medium"/>

        <!-- Back Button -->
        <Button
            style="@style/SecondaryButton"
            android:id="@+id/btn_back_to_main"
            android:text="Back to App"/>

    </LinearLayout>
</ScrollView>
